// This page has been moved to app/(public)/pricing/page.tsx
// This file exists only as a redirect to avoid broken imports.
// It should be deleted once confirmed the build passes.
import { redirect } from 'next/navigation';
export default function MarketingPricingRedirect() {
  redirect('/pricing');
}
