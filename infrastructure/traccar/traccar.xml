<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>

    <!-- SERVER -->
    <entry key='web.port'>8082</entry>
    
    <!-- DATABASE (Supabase PostGIS) -->
    <entry key='database.driver'>org.postgresql.Driver</entry>
    <entry key='database.url'>jdbc:postgresql://${SUPABASE_DB_HOST}:5432/${SUPABASE_DB_NAME}</entry>
    <entry key='database.user'>${SUPABASE_DB_USER}</entry>
    <entry key='database.password'>${SUPABASE_DB_PASSWORD}</entry>
    
    <!-- PROTOCOLS -->
    <!-- Disable unused protocols to save RAM. E.g. limit to osmand (5055) which is often used for app-based location -->
    <entry key='osmand.port'>5055</entry>

    <!-- We disable Geocoder/Reverse Geocoder to save Mapbox API calls -->
    <entry key='geocoder.enable'>false</entry>

</properties>
