<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE properties SYSTEM 'http://java.sun.com/dtd/properties.dtd'>
<properties>

    <entry key='config.default'>./conf/default.xml</entry>

    <!-- Use environment variables via Fly Secrets for DB config -->
    <!-- Traccar supports OS ENV mapping through ${ENV_VAR} -->
    <entry key='database.driver'>org.postgresql.Driver</entry>
    <entry key='database.url'>jdbc:postgresql://${DATABASE_HOST:-db.pooler.supabase.com}:5432/postgres</entry>
    <entry key='database.user'>${DATABASE_USER:-postgres}</entry>
    <entry key='database.password'>${DATABASE_PASSWORD:-password}</entry>

    <!-- Reverse Geocoding (Nominatim) -->
    <entry key='geocoder.enable'>true</entry>
    <entry key='geocoder.type'>nominatim</entry>
    <entry key='geocoder.url'>https://nominatim.openstreetmap.org/reverse</entry>
    <entry key='geocoder.onRequest'>true</entry>
    <entry key='geocoder.ignorePositions'>true</entry>
    <entry key='geocoder.reuseDistance'>10</entry>

</properties>
