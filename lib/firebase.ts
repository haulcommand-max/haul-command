/**
 * DEPRECATED: Firebase has been completely removed in favor of Supabase and Novu.
 * This file is kept as a stub to prevent broken imports in legacy components.
 */

export const app = null;
export const messaging = null;

export const requestFirebaseNotificationPermission = async () => null;
export const onMessageListener = () => new Promise(() => {});
export const trackEvent = (eventName: string, params?: any) => {};
