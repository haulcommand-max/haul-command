# ─── listmonk ───
LISTMONK_DB_PASSWORD=changeme_listmonk_db
LISTMONK_ADMIN_USER=admin
LISTMONK_ADMIN_PASS=changeme_admin
LISTMONK_ADMIN_EMAIL=admin@haulcommand.com

# ─── SMTP Provider (pick ONE, configure in listmonk admin UI too) ───
# Brevo (default tonight — free tier 300/day)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Amazon SES SMTP (recommended at scale — $0.10/1000)
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=

# Resend SMTP (dev-friendly — free 100/day)
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USER=resend
# SMTP_PASS=

# ─── From address ───
EMAIL_FROM_NAME="Haul Command"
EMAIL_FROM_ADDRESS=dispatch@haulcommand.com
EMAIL_REPLY_TO=support@haulcommand.com
