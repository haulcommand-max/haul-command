# Listmonk Email Engine — Environment Variables
# Copy this file to .env.email and fill in values before deploying.
#
# Deploy with:
#   docker compose -f infra/email/docker-compose.email.yml --env-file .env.email up -d
#   docker compose -f infra/email/docker-compose.email.yml run --rm listmonk ./listmonk --install

LISTMONK_DB_PASSWORD=hc_listmonk_s3cure_2026
LISTMONK_ADMIN_USER=admin
LISTMONK_ADMIN_PASS=HaulCommand2026!
