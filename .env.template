# ============================================================
# HAUL COMMAND — Final Stack Environment Variables
# Master template — all tools in their single lane
# ============================================================

# ── Vercel (Public Web) ──
NEXT_PUBLIC_APP_URL=https://haulcommand.com
NEXT_PUBLIC_APP_NAME=HAUL COMMAND

# ── Supabase (Canonical System of Record) ──
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Firebase (Mobile Engagement + Anti-Abuse) ──
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_VAPID_KEY=

# ── Typesense (Only Search Engine) ──
TYPESENSE_HOST=
TYPESENSE_PORT=443
TYPESENSE_PROTOCOL=https
TYPESENSE_API_KEY=
NEXT_PUBLIC_TYPESENSE_SEARCH_KEY=

# ── OpenAI (Intelligence Layer) ──
OPENAI_API_KEY=

# ── PostHog (Analytics, Flags, Experiments, Surveys, Replay) ──
# Replaces: GA4, Plausible, GrowthBook, Formbricks
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ── Langfuse (LLM Observability) ──
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_BASE_URL=https://cloud.langfuse.com

# ── Trigger.dev (Background Jobs) ──
TRIGGER_API_KEY=
TRIGGER_API_URL=https://api.trigger.dev

# ── Resend (Transactional Email) ──
RESEND_API_KEY=
RESEND_FROM_EMAIL=dispatch@haulcommand.com

# ── Listmonk (Newsletter & Campaigns) ──
LISTMONK_API_URL=
LISTMONK_API_USER=
LISTMONK_API_PASSWORD=

# ── Stripe (Fiat Billing) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# ── NOWPayments (Crypto Billing) ──
NOWPAYMENTS_API_KEY=
NOWPAYMENTS_IPN_SECRET=

# ── Upstash (Redis — Rate Limiting & Caching) ──
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ── Dub (Short Links, QR, Referrals) ──
DUB_API_KEY=
DUB_WORKSPACE_SLUG=haulcommand

# ── Documenso (Digital Signatures) ──
DOCUMENSO_API_KEY=
DOCUMENSO_API_URL=https://app.documenso.com/api/v1

# ── Lago (Usage-Based Billing) ──
LAGO_API_KEY=
LAGO_API_URL=https://api.getlago.com/api/v1

# ── Infisical (Secrets Management — Self-Hosted Layer) ──
INFISICAL_TOKEN=
INFISICAL_API_URL=https://app.infisical.com/api

# ── Twenty (CRM — Self-Hosted) ──
TWENTY_API_URL=https://crm.haulcommand.com
TWENTY_API_KEY=

# ── Sentry (Error Tracking) ──
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=

# ── Vapi (Voice AI) ──
VAPI_API_KEY=
VAPI_PHONE_NUMBER_ID=

# ── ElevenLabs (Voice Synthesis) ──
ELEVENLABS_API_KEY=

# ── MapLibre (Maps) ──
NEXT_PUBLIC_MAPLIBRE_STYLE_URL=

# ============================================================
# REMOVED TOOLS — DO NOT ADD BACK
# ============================================================
# Meilisearch — removed (Typesense owns search)
# Novu — removed (Firebase + Resend + Listmonk cover notifications)
# Rallly — removed (no critical lane)
# GoHighLevel — removed (Twenty replaces CRM)
# GA4 — removed (PostHog is canonical analytics)
# Plausible — removed (PostHog owns analytics)
# GrowthBook — removed (PostHog owns flags/experiments)
# Formbricks — removed (PostHog owns surveys)
# Cal.com — removed (not a core layer)
